- defaults:
    link:
      relink: true

- link:
    ~/.bash_aliases: link_files/bash_aliases
    ~/.bashrc_shared: link_files/bashrc_shared
    ~/.gitignore: link_files/gitignore
    ~/.gitconfig_shared: link_files/gitconfig_shared
    ~/.vimrc: link_files/vimrc
    ~/.bin: bin
    ~/.vim: vim
