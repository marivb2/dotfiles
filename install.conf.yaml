- defaults:
    link:
      relink: true

- link:
    ~/.bash_aliases: bash_aliases
    ~/.bashrc_shared: bashrc_shared
    ~/.bin: bin
    ~/.gitignore: gitignore
    ~/.gitconfig_shared: gitconfig_shared
    ~/.vim: vim
    ~/.vimrc: vimrc
